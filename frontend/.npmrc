# Force npm to include all optional dependencies
optional=true

# Use legacy peer deps resolution
legacy-peer-deps=true

# Auto install peer dependencies
auto-install-peers=true

# Save exact versions
save-exact=false
